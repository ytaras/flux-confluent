- op: add
  path: /spec/values/global/provider
  value:
    name: private
    ## if any name which indicates regions
    ##
    region: anyregion
    ##
    ## Docker registry endpoint where Confluent Images are available.
    ##
    registry:
      fqdn: docker.io
      credential:
        required: false